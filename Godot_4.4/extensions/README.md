This will be base classes and samples that use them.

I will plan to add more as I clean up the code, but this should have things like extended character, save states, and resource base (slotable) components. 
Most fo these will be more of an example, so they may have extra features to support standalong behaviors without having to extend or modify the class
